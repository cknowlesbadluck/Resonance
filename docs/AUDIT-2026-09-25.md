# Resonance Product Audit — 2026-09-25

Main: `d9dc1e62` (fail-closed + capability plane). CI #892 success. Open PRs: 0. Extra branches: `develop`, `feature/ios-p4-compose-execute-evidence`.

Live host remains `https://resonancenexus.netlify.app`. `/api/ready` returns 503. Do not switch hosts.

## Executive read

- Maturity: alpha control plane, demo host.
- Binding constraint: production env unset on the existing Netlify site.
- Honest verdict: main is ahead of ops. Linear Completion on 2026-09-23 was false. IMPLEMENTATION_STATUS on main still cited SHA `5e427d85` after `d9dc1e62` landed. That is documentation rot, not a finished product.

## Current vs finished (0–5)

| Area | Score | On main? | Gap |
|---|---:|---|---|
| Governance and CI | 4 | yes | production-smoke skipped |
| Auth, idempotency, policy | 4 | yes | live authMode still auto until env set |
| Durable execution | 2 | code yes | no live persistence |
| Capability plane | 4 | yes #102 | live rows + second adapter |
| GitHub adapter slice | 3 | yes | no durable live evidence |
| Web UX | 3 | yes | host not ready |
| Native iOS peer | 2 | package tests | no Xcode target proof, ios-p4 branch orphan |
| Chamber fabric | 2 | in-process | not persisted |
| Multi-adapter | 1 | fixtures | unpublished |
| Release readiness | 2 | health 200 | ready 503 |

## What is shipped on main

Fail-closed user-data routes without persistence/required auth. Webhook ack only after signature + durable insert. Partial DAG status. NexusCapability executable flag. Swift package tests in CI.

## What is not shipped

Ready host. Durable GitHub read after restart. Physical iPhone loop. Second provider. Chamber persistence. Signed IPA.

## Possibilities

### Path A — Ops unlock (default)
Set env on resonancenexus only. Apply migrations. Prove one durable read.

### Path B — Native loop on a mock host
Keeps iOS moving, lies about production.

### Path C — More adapters first
Increases surface area on a 503 host. Reject this cycle.

## Phase plan (portfolio Phases 1, 2, 6, 7, 8)

1. `/api/ready` 200, auth required.
2. Durable `github.repository.read` evidence.
6. iOS I0–I1 against that host; salvage or delete ios-p4.
7. Second real adapter.
8. Chamber durability.

Exit criteria must cite live HTTP and stored rows, not CI.
