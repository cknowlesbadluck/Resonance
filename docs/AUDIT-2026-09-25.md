# Resonance / portfolio audit — 2026-09-25 18:03 EDT

Single current audit. Hourly PORTFOLIO-AUDIT-* files are superseded.

## Binding constraint

Live host `https://resonancenexus.netlify.app/api/ready` returns 503.

```json
{"status":"not_ready","missingRequired":["SUPABASE_SERVICE_ROLE_KEY"],"authMode":"required","authModeOk":true,"persistenceConfigured":false,"githubAdapterConfigured":false}
```

Netlify site `resonancenexus` id `7fc56cb3-d5f7-4bb2-8986-a733b8cfd548` has AUTH_MODE, DEPLOY_STAGE, public Supabase URL + anon. It does not have SERVICE_ROLE or GITHUB_TOKEN. Do not invent them. Do not switch to `resonance-nexus` or `resonanceplane`.

## Main

`5bc73db4` after #110. Closed #111 (duplicate 17:00 docs). web+ios CI green on that docs PR; production-smoke skipped because host is not ready.

Fail-closed user-data and ready-or-refuse are on main. They are not proven against persistence until SERVICE_ROLE is set and migrations applied.

## Scores vs finished vision (0–5)

| Area | Score | Gap |
| --- | --- | --- |
| Governance / CI truth | 4 | Required web+ios green; production-smoke skipped |
| Auth, idempotency, policy | 4 | Contracts on main; live persistence missing |
| Durable execution / evidence | 2 | Code exists; live store not configured |
| Capability plane | 3 | On main, thin vs finished |
| GitHub adapter slice | 2 | Code + CI token tests; live adapter unset |
| Web control plane | 3 | Ready-or-refuse works; execute path locked |
| Native iOS peer | 2 | Package tests; I1 blocked on ready host |
| Chamber fabric | 2 | Spec/partial; not live |
| Multi-adapter | 1 | One real adapter, not live |
| Release readiness | 2 | Secrets + smoke + entropy |

Maturity: beta control plane, not production. Binding constraint = CHR-54.
